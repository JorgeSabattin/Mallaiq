[tool.poetry]
name = "mallaiq"
version = "1.0.0"
description = "MallaIQ SaaS"

[tool.poetry.dependencies]
python = "3.11.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"